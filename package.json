{
  "name": "ralli",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate",
    "db:seed": "turbo run db:seed",
    "db:seed:teams-athletes": "turbo run db:seed:teams-athletes",
    "db:seed:update-stats": "turbo run db:seed:update-stats",
    "fetch:athletes": "turbo run fetch:athletes",
    "update-types": "cd apps/server && pnpm build:types && cd ../../apps/web && pnpm install"
  },
  "devDependencies": {
    "prettier": "^3.5.3",
    "turbo": "^2.5.4",
    "typescript": "5.8.2",
    "drizzle-kit": "^0.31.4",
    "tsup": "^8.5.0",
    "drizzle-orm": "^0.44.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "postgres": "^3.4.7",
    "next": "15.3.3"
  }
}
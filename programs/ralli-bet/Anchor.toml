[toolchain]

[features]
resolution = true
skip-lint = false

[programs.localnet]
ralli_bet = "GdZAn63TbgoMySA4B2N7caWo6ujEU9qKyKyoRcs71zAp"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "npx ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
test-batch = "npx ts-mocha -p ./tsconfig.json -t 1000000 tests/batch_resolution_tests.ts"
test-main = "npx ts-mocha -p ./tsconfig.json -t 1000000 tests/ralli-bet.ts"
